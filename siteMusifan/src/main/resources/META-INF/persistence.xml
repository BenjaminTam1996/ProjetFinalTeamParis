<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.0"
	xmlns="http://java.sun.com/xml/ns/persistence"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd">

	<!-- définition de l'unité de persistence => info de connection -->
	<persistence-unit name="siteMusifan"
		transaction-type="RESOURCE_LOCAL">

		<!-- implementation de JPA -->
		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

		<!-- dépendent de l'implementation -->
		<properties>
			<!-- Mise en place du context pour l'échange avec postgresql -->
			<property name="hibernate.connection.driver"
				value="org.postgresql.Driver" />
			<property name="hibernate.connection.url"
				value="jdbc:postgresql://localhost:5432/site_musifan" />
			<property name="hibernate.connection.user" value="postgres" />
			<property name="hibernate.connection.password"
				value="postgres" />

			<!-- définition du dialect pour échanger avec la base de données -->
			<property name="hibernate.dialect"
				value="org.hibernate.dialect.PostgreSQL10Dialect" />

			<!-- values possible : validate, create, create-drop, update -->
			<property name="hibernate.hbm2ddl.auto" value="create" />

			<!-- Permet d'afficher toutes les requetes sql -->
			<property name="hibernate.show_sql" value="true" />

			<!-- Permet de formater les requetes sql -->
			<property name="hibernate.format_sql" value="true" />
		</properties>
	</persistence-unit>
</persistence>